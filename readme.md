# BookStack Customize - Header links

Add tag page to the header links.  
This module replaces the existing template rather than adding to it.  
Be careful to avoid incompatibilities caused by version differences.  
